<!-- Menu principal -->
<div class="col-md-7 d-flex justify-content-center align-items-center">
    <nav class="main-menu">
        <ul class="list-unstyled d-flex justify-content-center gap-4 mb-0">
            <li><a href="{{ route('home') }}" class="fw-bold">{{ __('messages.home') }}</a></li>
            <li><a href="{{ route('marketplace.items.index') }}" class="fw-bold">{{ __('messages.marketplace') }}</a></li>
            <li><a href="{{ route('forums.index') }}" class="fw-bold">{{ __('messages.forums') }}</a></li>
            <li><a href="{{ route('about') }}" class="fw-bold">{{ __('messages.about') }}</a></li>
            <li><a href="{{ route('contact') }}" class="fw-bold">{{ __('messages.contact') }}</a></li>
        </ul>
    </nav>
</div>







